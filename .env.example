# Database - 仅 TiDB Cloud（不要配置 POSTGRES_* 等旧变量，避免连错库）
# 在 TiDB Cloud 控制台：Clusters -> 你的集群 -> Connect -> Prisma，复制连接串，末尾加 ?sslaccept=strict
DATABASE_URL="mysql://user:password@gateway01.区域.prod.aws.tidbcloud.com:4000/immigration_assistant?sslaccept=strict"

# Application
NODE_ENV="development"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Session
SESSION_SECRET="your-super-secret-session-key-change-in-production"

# Email (可选，用于发送验证码)
# SMTP_HOST="smtp.example.com"
# SMTP_PORT="587"
# SMTP_USER="your-email@example.com"
# SMTP_PASS="your-email-password"
# EMAIL_FROM="noreply@example.com"
