# Database - 仅 TiDB Cloud（不要配置 POSTGRES_* 等旧变量，避免连错库）
# 在 TiDB Cloud 控制台：Clusters -> 你的集群 -> Connect -> Prisma，复制连接串，末尾加 ?sslaccept=strict
DATABASE_URL="mysql://user:password@gateway01.区域.prod.aws.tidbcloud.com:4000/immigration_assistant?sslaccept=strict"

# Application
NODE_ENV="development"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
# 邮件中的验证/重置链接域名。生产环境不设则自动用 https://www.jiayi.co（国内用户请勿用 *.vercel.app）
# APP_URL="https://www.jiayi.co"

# Session
SESSION_SECRET="your-super-secret-session-key-change-in-production"

# Email (可选，用于发送验证码)
# SMTP_HOST="smtp.example.com"
# SMTP_PORT="587"
# SMTP_USER="your-email@example.com"
# SMTP_PASS="your-email-password"
# EMAIL_FROM="noreply@example.com"

# 百度链接提交（推送收录）- 在百度搜索资源平台「普通收录」-「API 提交」获取
# BAIDU_PUSH_SITE="https://www.jiayi.co"
# BAIDU_PUSH_TOKEN="your-token-from-baidu"

# IRCC 新闻页英译中（可选）- LibreTranslate 公共 API 或自建实例
# 不配置则仅显示英文；配置后自动翻译为中文
# LIBRE_TRANSLATE_URL="https://libretranslate.com"
# LIBRE_TRANSLATE_API_KEY="your-key"

# 新用户/新顾问注册时推送到企业微信群（可选）
# 在企业微信群中添加「群机器人」获取 Webhook 地址，格式：https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxxxx
# WECHAT_WEBHOOK_URL="https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxxxx"
